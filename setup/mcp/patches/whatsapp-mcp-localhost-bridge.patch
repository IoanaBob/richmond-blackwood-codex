diff --git a/.gitignore b/.gitignore
index fdecba8..37f3de8 100644
--- a/.gitignore
+++ b/.gitignore
@@ -1,3 +1,6 @@
 *.db
+.venv/
+__pycache__/
+*.pyc
 claude_desktop_config.json
-whatsapp.log
\ No newline at end of file
+whatsapp.log
diff --git a/whatsapp-bridge/main.go b/whatsapp-bridge/main.go
index e7e30f5..b25fecc 100644
--- a/whatsapp-bridge/main.go
+++ b/whatsapp-bridge/main.go
@@ -13,6 +13,7 @@ import (
 	"os/signal"
 	"path/filepath"
 	"reflect"
+	"strconv"
 	"strings"
 	"syscall"
 	"time"
@@ -774,8 +775,20 @@ func startRESTServer(client *whatsmeow.Client, messageStore *MessageStore, port
 		})
 	})
 
+	// Keep the local bridge private by default. Override only for explicit local debugging.
+	host := os.Getenv("WHATSAPP_BRIDGE_HOST")
+	if host == "" {
+		host = "127.0.0.1"
+	}
+
+	if envPort := os.Getenv("WHATSAPP_BRIDGE_PORT"); envPort != "" {
+		if parsedPort, err := strconv.Atoi(envPort); err == nil {
+			port = parsedPort
+		}
+	}
+
 	// Start the server
-	serverAddr := fmt.Sprintf(":%d", port)
+	serverAddr := fmt.Sprintf("%s:%d", host, port)
 	fmt.Printf("Starting REST API server on %s...\n", serverAddr)
 
 	// Run server in a goroutine so it doesn't block

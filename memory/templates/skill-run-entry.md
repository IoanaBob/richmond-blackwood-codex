# Skill Run Entry Template

Append one row to `memory/skill-runs.md` when a repo-local or plugin skill is used.

```markdown
| YYYY-MM-DD HH:MM TZ | `skill-name` | Short task name | Why the skill was used. | What it produced or changed. | Verification or known gaps. |
```

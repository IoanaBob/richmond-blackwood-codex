interface:
  display_name: "Inbound Operating Triage"
  short_description: "Run RB inbound triage as a gated flow of capture, classification, routing, closeout, and approval."
  default_prompt: "Use $rb-inbound-operating-triage to run the inbound triage flow: capture Gmail/WhatsApp sources, classify each item, route finance and task/correspondence work to the right subskills, prepare the Slack closeout preview, and request approval before any send."

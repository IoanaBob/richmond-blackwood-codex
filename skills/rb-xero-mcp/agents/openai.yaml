display_name: Xero MCP
short_description: Use the local Xero MCP server for explicit-client RB Xero reads and approved writes.
default_prompt: Ask for the exact RB client reference, ensure the active local Xero client matches it, verify the Xero organisation first, and only perform approved Xero reads or writes for that client.

display_name: Xero MCP
short_description: Use client-specific Xero MCP servers for RB Xero reads and approved writes.
default_prompt: Ask for the exact RB client reference, verify the Xero organisation through the matching MCP server first, and only perform approved Xero reads or writes for that client.
